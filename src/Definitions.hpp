#ifndef DEFINITIONS_HPP
#define DEFINITIONS_HPP

enum class ProfessionType {
    None,   // Init value
    CraneOperator,
    DefaultWorker
};

enum class WorkingShiftType {
    None,   // Init value
    EarlyShift,
    LateShift
};

#endif // DEFINITIONS_HPP
